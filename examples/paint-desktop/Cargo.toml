[package]
name = "paint-desktop"
version.workspace = true
edition.workspace = true

[dependencies]
rinch = { workspace = true, features = ["desktop", "components", "theme"] }
paint = { path = "../paint" }
