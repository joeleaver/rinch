[package]
name = "rinch-platform"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Platform abstraction traits for rinch"

[dependencies]
vello = { workspace = true }
peniko = "0.6"

[target.'cfg(target_arch = "wasm32")'.dependencies]
web-time = "1"
